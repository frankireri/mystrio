// lib/config/app_config.dart

class AppConfig {
  // The base URL for constructing shareable links.
  // This can be easily changed for different environments (e.g., dev, staging, prod).
  static const String baseUrl = 'mystrio.app';
}
